// Application — слой use-cases (сценарии-оркестраторы) и граница транзакционности.
//
// Импортирует только Domain. Бизнес-правила живут в Domain (сущности/сервисы);
// здесь — оркестрация вызовов и гарантия согласованности изменений как единого целого.
//
// Пока пусто: наполняется в последующих фазах (RunService и др.).
// Файл держит таргет непустым, чтобы SPM собирал модуль.
