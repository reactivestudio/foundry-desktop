/// Конструктор внедрения (аналог Spring `@Autowired` на конструкторе): помечает, КАКИМ init'ом
/// контейнер строит бин. Нужен ровно тогда, когда init'ов несколько — иначе скан не может знать, что
/// перед ним: точка внедрения или, скажем, тестовый/конвенциональный конструктор. Один init — маркер
/// не нужен (как в Spring: единственный конструктор внедряется без аннотации). Несколько init'ов без
/// маркера — ОШИБКА СБОРКИ, а не догадка сканера.
///
/// Чистый МАРКЕР: ничего не разворачивает, определение бина целиком генерит `BeanScan`.
@attached(peer)
public macro Autowired() =
    #externalMacro(module: "SwiftContextMacros", type: "ComponentMacro")
