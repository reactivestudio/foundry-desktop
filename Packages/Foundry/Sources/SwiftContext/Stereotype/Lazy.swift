/// Ленивый бин (аналог Spring `@Lazy`): не строить при сборке контекста, а отложить до первого
/// резолва — бин уходит из жадной преинстанциации. Волей АВТОРА, в отличие от
/// `BeanDefinition.isMainActorConfined`, который выставляет кодоген по системе типов.
///
/// Цена — потеря fail-fast: кривая проводка такого бина всплывёт не на старте, а при первом
/// обращении. Поэтому по умолчанию бины жадные (как в Spring) — метить лениво стоит только дорогое
/// на старте и редко используемое.
///
/// Чистый МАРКЕР: определение бина (`isLazyInit`) генерит `BeanScan` по результату скана.
@attached(peer)
public macro Lazy() =
    #externalMacro(module: "SwiftContextMacros", type: "ComponentMacro")
