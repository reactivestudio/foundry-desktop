/**
 Доменное событие — факт, случившийся в домене («тему сменили», «профиль
 переименовали»). Маркерный протокол общего словаря: конкретные события объявляют
 контексты в своём Domain-слое. Корень агрегата копит их (`AggregateRoot.record`),
 use-case выпускает после транзакции. `Sendable` — событие безопасно пересекает
 акторы при публикации.
 */
public protocol DomainEvent: Sendable {}
